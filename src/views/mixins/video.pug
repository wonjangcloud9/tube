mixin video(video)
    div 
        video(src="/" + video.fileUrl controls width="1080" height="720")
        h4 
            a(href="/videos/" + video.id)=video.title
        p=video.description
        ul 
            each hashtag in video.hashtags
                li=hashtag
        small=video.createdAt
        hr